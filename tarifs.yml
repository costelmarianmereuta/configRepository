ms:
    kafkaTopicName: tarifsRemoveTopic
server:
    port: 8082
spring:
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        password: root
        url: jdbc:mysql://localhost:3306/tarifs?zeroDateTimeBehavior=CONVERT_TO_NULL&serverTimezone=UTC
        username: root
    jpa:
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        hibernate:
            ddl-auto: update
